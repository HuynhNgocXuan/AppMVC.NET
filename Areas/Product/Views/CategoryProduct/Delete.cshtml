@model webMVC.Models.Product.CategoryProduct

@{
    ViewData["Title"] = "Xóa chuyên mục";
}


<h2 class="my-3">Bạn có muốn chắc chắn xóa chuyên mục?</h2>
<div>
    <dl class="row ">
        <dt class="col-md-4">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class="col-md-8">
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Xóa" class="btn btn-danger me-3" />
        <a class="btn btn-primary" asp-action="Index">Danh sách chuyên mục Blog</a>
    </form>
</div>
